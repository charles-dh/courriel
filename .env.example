# Courriel Environment Variables
#
# Copy this file to .env and fill in your values.
# The .env file is gitignored to prevent committing secrets.

# Microsoft 365 client secret (from Azure app registration)
# Using an environment variable is preferred over storing in config.toml
# COURRIEL_MS365_CLIENT_SECRET=your-client-secret-here

# Gmail client secret (from Google Cloud Console OAuth credentials)
# Using an environment variable is preferred over storing in config.toml
# COURRIEL_GMAIL_CLIENT_SECRET=your-gmail-client-secret-here
